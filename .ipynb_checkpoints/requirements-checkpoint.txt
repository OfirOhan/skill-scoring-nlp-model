# ── Scoring model + generation ──────────────────────────────
aiohttp
tqdm
coral_pytorch
numpy
pandas
scikit-learn
scipy
torch
transformers
accelerate        # Qwen3-Reranker (CausalLM) device placement
tiktoken          # DeBERTa-v3 tokenizer conversion (transformers 5.x)
sentencepiece     # DeBERTa-v3 tokenizer (SentencePiece-based)
protobuf          # needed when converting the DeBERTa-v3 tokenizer

# ── RAG (ingestion + retrieval) ─────────────────────────────
# Required to run rag/ and scoring_model/build_dataset.py.
chromadb
sentence-transformers
rank-bm25
langchain-text-splitters
unstructured
ollama
einops            # nomic-embed-text-v1.5 (trust_remote_code) needs this
